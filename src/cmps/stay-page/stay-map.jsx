import { useState } from "react"


export const StayMap = (props) => {
    const [stay, setStay] = useState(props.stay)



    return (
        <section className="stay-map">

        </section>
    )
}